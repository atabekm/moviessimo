package com.example.core.usf

/**
 * This a parent interface from which all Events should derive. Event is the entry point in unidirectional
 * state flow pattern, i.e. every action that requires some attention needs to be triggered via events.
 */
interface ViewEvent
