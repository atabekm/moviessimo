package com.example.core.mvi

/**
 * This a parent interface from which all Actions should derive. Action is the entry point in unidirectional
 * state flow pattern, i.e. every event that requires some attention needs to be triggered via actions.
 */
interface MviAction
